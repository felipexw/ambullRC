package com.example.ambullrc.model

/**
 * The four discrete directions the control screen exposes. Each on-screen button maps to
 * exactly one value, and each logged record references exactly one value.
 */
enum class Direction {
    UP,
    DOWN,
    LEFT,
    RIGHT
}
